/**
 * 配置文件
 */
const CONFIG = {
    // API基础URL，根据实际部署修改
    API_BASE_URL: 'http://localhost:8080',
    
    // 数据刷新间隔（毫秒）
    REFRESH_INTERVAL: 30000,
    
    // 地图配置
    MAP_CONFIG: {
        center: [116.404, 39.915], // 默认中心点（北京）
        zoom: 13
    }
};
