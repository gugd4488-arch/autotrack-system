server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/autotrack?useSSL=false&serverTimezone=UTC
    username: root
    password: autotrack2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

autotrack:
  gps:
    port: 5055
    protocol: osmand
  scripts:
    storage: /data/scripts
  traccar:
    enabled: true
